<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="text">
      <p>COMMENT on Post:</p>
      {{comment_text}}
    </div>
    <div class="meta">
      by: {{user.username}} on {{format_date created_at}}
    </div>
  </section>
  {{/each}}
</div>